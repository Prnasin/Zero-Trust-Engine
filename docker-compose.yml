version: "3.9"

services:
  mongo:
    image: mongo
    container_name: zero-trust-mongo
    restart: always
    ports:
      - "27019:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data: